name: dartscl_protocol
description: Shared models for DartSCL
version: 1.0.0
resolution: workspace

environment:
  sdk: '^3.5.0'

dependencies:
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.16.0
  json_serializable: ^6.14.1